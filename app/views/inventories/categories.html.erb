<p id="notice"><%= notice %></p>

	<div class="container">
		<h1 class="bg-info" align="center">분류명 목록</h1>

			<table class="table table-hover">
				<thead>
					<tr>
						<th>고유번호</th>
						<th>분류 명</th>
	    		</tr>
				</thead>

				<tbody>
					<% @categories.each do |c| %>
	  				<tr>
              <td><%= c.id %></td>
	  					<td><%= c.name %></td>
	  				</tr>
	    		<% end %>
				</tbody>
			</table>

      <button type="button" class="btn btn-default"><a href="/category_write">카테고리 등록</a></button>
      <button type="button" class="btn btn-default"><%= link_to '뒤로 가기', '/setting_page' %></button>
    </div>
